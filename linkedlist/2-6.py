def isPalindrome (sequence):
	p1 = sequence
	p2 = None
	
	

	# get p2 to the end, probably wouldn't work though since we are not working with a doubly linked list
	while temp.next != None:
		p2 = temp
		temp = temp.next
	
	 
